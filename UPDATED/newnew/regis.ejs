<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login Page</title>
    <style>
        /* Basic styling - add more as needed */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .login-container {
            display: flex;
            flex-direction: row; /* Align elements horizontally */
        }
        .login-form {
            background-color: #fff;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            text-align: center;
        }
        h2 {
            margin-bottom: 30px;
            color: #333;
        }
        input[type="email"],
        input[type="password"],
        button {
            width: calc(100% - 22px);
            padding: 12px;
            margin-bottom: 20px;
            border-radius: 4px;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }
        a {
            text-decoration: none;
            color: #1db954;
        }

         a:hover{
            text-decoration: none;
            color: #038903;
        }
        button {
            padding: 10px 20px;
            border: 20px;
            border-radius: 4px;
            background-color: #1db954 ;
            color: #fff;
            cursor: pointer;
        }
        button:hover {
            background-color: #038903;
        }
        .forgot-password {
            text-align: left;
            margin-bottom: 20px;
            margin-left: 10px;
        }
        .divider {
            margin: 10px 0;
            border-bottom: 1px solid #ccc;
        }
        .image-container {
            margin-right: 250px;
            flex: 1; 
            max-width: 400px; 
        }
        .image {
            width: 199%; 
            height: 100%; 
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="login-container">
            <!-- Image container -->
            <div class="image-container">
                <!-- Replace 'path/to/your/image.jpg' with your image URL -->
                <img src="ictc.png" alt="ICTC" class="image">

            </div>

            <!-- Login form -->
            <div class="login-form">
                <h2>Create an Account</h2>
                <form action="your-login-endpoint" method="POST">
                    <input type="email" id="email" name="email" placeholder="Email Address" required><br><br>
                    <input type="password" id="password" name="password" placeholder="Password" required><br><br>
                    <button type="submit">Continue</button><br><br>
                </form>
                <div class="divider"></div>
                <p>Already have an account? <a href="mainlogin.ejs">Login</a></p>
            </div>
        </div>
    </div>
</body>
</html>
